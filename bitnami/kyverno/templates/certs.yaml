{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.kyverno.tls.autoGenerated.enabled (eq .Values.kyverno.tls.autoGenerated.engine "cert-manager") }}
{{- if (not .Values.admissionController.tls.existingSecret) -}}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ printf "%s-crt" (include "kyverno.admission-controller.fullname" .) }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: admission-controller
    app.kubernetes.io/part-of: kyverno
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  secretName: {{ printf "%s-crt" (include "kyverno.admission-controller.fullname" .) }}
  commonName: {{ printf "%s.%s.svc.%s" (include "kyverno.admission-controller.fullname" .) (include "common.names.namespace" .) .Values.clusterDomain }}
  issuerRef:
    name: {{ printf "%s-ca-issuer" (include "common.names.fullname" .) }}
    kind: Issuer
  subject:
    organizations:
      - "kyverno"
  dnsNames:
    - '*.{{ include "common.names.namespace" . }}'
    - '*.{{ include "common.names.namespace" . }}.svc'
    - '*.{{ include "common.names.namespace" . }}.svc.{{ .Values.clusterDomain }}'
    - '*.{{ include "kyverno.admission-controller.fullname" . }}'
    - '*.{{ include "kyverno.admission-controller.fullname" . }}.{{ include "common.names.namespace" . }}'
    - '*.{{ include "kyverno.admission-controller.fullname" . }}.{{ include "common.names.namespace" . }}.svc'
    - '*.{{ include "kyverno.admission-controller.fullname" . }}.{{ include "common.names.namespace" . }}.svc.{{ .Values.clusterDomain }}'
  privateKey:
    algorithm: {{ .Values.kyverno.tls.autoGenerated.certManager.keyAlgorithm }}
    size: {{ int .Values.kyverno.tls.autoGenerated.certManager.keySize }}
  duration: {{ .Values.kyverno.tls.autoGenerated.certManager.duration }}
  renewBefore: {{ .Values.kyverno.tls.autoGenerated.certManager.renewBefore }}
{{- end }}
{{- if (not .Values.cleanupController.tls.existingSecret) -}}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ printf "%s-crt" (include "kyverno.cleanup-controller.fullname" .) }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: cleanup-controller
    app.kubernetes.io/part-of: kyverno
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  secretName: {{ printf "%s-crt" (include "kyverno.cleanup-controller.fullname" .) }}
  commonName: {{ printf "%s.%s.svc.%s" (include "kyverno.cleanup-controller.fullname" .) (include "common.names.namespace" .) .Values.clusterDomain }}
  issuerRef:
    name: {{ printf "%s-ca-issuer" (include "common.names.fullname" .) }}
    kind: Issuer
  subject:
    organizations:
      - "kyverno"
  dnsNames:
    - '*.{{ include "common.names.namespace" . }}'
    - '*.{{ include "common.names.namespace" . }}.svc'
    - '*.{{ include "common.names.namespace" . }}.svc.{{ .Values.clusterDomain }}'
    - '*.{{ include "kyverno.cleanup-controller.fullname" . }}'
    - '*.{{ include "kyverno.cleanup-controller.fullname" . }}.{{ include "common.names.namespace" . }}'
    - '*.{{ include "kyverno.cleanup-controller.fullname" . }}.{{ include "common.names.namespace" . }}.svc'
    - '*.{{ include "kyverno.cleanup-controller.fullname" . }}.{{ include "common.names.namespace" . }}.svc.{{ .Values.clusterDomain }}'
  privateKey:
    algorithm: {{ .Values.kyverno.tls.autoGenerated.certManager.keyAlgorithm }}
    size: {{ int .Values.kyverno.tls.autoGenerated.certManager.keySize }}
  duration: {{ .Values.kyverno.tls.autoGenerated.certManager.duration }}
  renewBefore: {{ .Values.kyverno.tls.autoGenerated.certManager.renewBefore }}
{{- end }}
{{- end }}