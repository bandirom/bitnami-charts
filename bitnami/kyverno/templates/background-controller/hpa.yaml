{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.backgroundController.enabled .Values.backgroundController.autoscaling.hpa.enabled }}
apiVersion: {{ include "common.capabilities.hpa.apiVersion" ( dict "context" $ ) }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ template "kyverno.background-controller.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/part-of: kyverno
    app.kubernetes.io/component: background-controller
  {{- if or .Values.commonAnnotations .Values.backgroundController.autoscaling.hpa.annotations }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.backgroundController.autoscaling.hpa.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: {{ template "kyverno.background-controller.fullname" . }}
  minReplicas: {{ .Values.backgroundController.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.backgroundController.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- if .Values.backgroundController.autoscaling.hpa.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.backgroundController.autoscaling.hpa.targetCPU }}
    {{- end }}
    {{- if .Values.backgroundController.autoscaling.hpa.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.backgroundController.autoscaling.hpa.targetMemory }}
    {{- end }}
{{- end }}
